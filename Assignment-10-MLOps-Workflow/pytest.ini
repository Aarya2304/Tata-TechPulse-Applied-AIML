[pytest]
testpaths = tests
# -p no:flask: the globally installed pytest-flask plugin is incompatible
# with the installed Flask version and crashes pytest at startup on this
# machine; this project does not use it.
addopts = -p no:cacheprovider -p no:flask
