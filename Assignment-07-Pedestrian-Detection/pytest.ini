[pytest]
testpaths = tests
# "-p no:flask" disables the globally installed (broken) pytest-flask plugin,
# which crashes pytest at startup with the current Flask version. This project
# does not use Flask; the flag has no other effect.
addopts = -q -p no:flask
